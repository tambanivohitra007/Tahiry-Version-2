// ------------------------------------------------------------
// Author: Rindra Razafinjatovo
// Created on: 2018
// Last Modified: Dec 2024
// Project: Tahiry
// Description: A collection of Bible and Hymnals to streamline and enhance worship presentations for churches.
// ------------------------------------------------------------

﻿namespace Fihirana_database.Trunc
{
    internal class Trash
    {
        //else
        //{
        //    test.Add(getText);
        //    getText = "";
        //}

        //if (item.Range.Length == 1 && !string.IsNullOrEmpty(getText))
        //{
        //    //length = item.Range.End.ToInt() - 1 - start;
        //    //DocumentRange range_ = richEdit.Document.CreateRange(start, length);
        //    //getParoles.Add(new Chant() { Parole = richEdit.Document.GetText(range_) });

        //    ////richEdit.Document.Selections.Add(range_);
        //    //start = item.Range.End.ToInt();

        //    getParoles.Add(new Chant() { Parole = getText });
        //    getText = "";
        //}
        //else
        //{
        //    start = item.Range.Start.ToInt();
        //    DocumentRange range_ = richEdit.Document.CreateRange(start, item.Range.Length - 1);
        //    getText += richEdit.Document.GetText(range_) + "\n";
        //}

        //richEdit.Document.InsertText(item.Range.End, Environment.NewLine);

        //length = lastFound.Range.End.ToInt() - 1 - start;
        //DocumentRange range_ = richEdit.Document.CreateRange(start, length);
        //getParoles.Add(new Chant() { Parole = richEdit.Document.GetText(range_) });

        //richEdit.Document.Selections.Add(range_);

        //if (!item.Equals(lastFound))
        //{
        //    if (item.Range.Length <= 1)
        //    {
        //        length = item.Range.End.ToInt() - start;
        //        DocumentRange range_ = richEdit.Document.CreateRange(start, length);
        //        //getParoles.Add(new Chant() { Parole = richEdit.Document.GetText(range_) });

        //        richEdit.Document.Selections.Add(range_);
        //        start = item.Range.End.ToInt() - 1;
        //    }
        //}
        //else if (item.Equals(lastFound) && lastFound.Range.Length > 1)
        //{
        //    richEdit.Document.InsertText(item.Range.End, Environment.NewLine);
        //}

        //foreach (var item in found)
        //{
        //    if(item.Equals(lastFound))
        //    {
        //        length = item.End.ToInt() - 1 - start;
        //        DocumentRange range_ = richEdit.Document.CreateRange(start, length);
        //        getParoles.Add(new Chant() { Parole = richEdit.Document.GetText(range_) });

        //        //richEdit.Document.Selections.Add(range_);
        //        start = item.End.ToInt();
        //    }

        //}

        //for (int i = 0; i < found.Length; i++)
        //{
        //    if (i!= found.Length - 2)
        //    {
        //        int length = found[i].End.ToInt() - start;
        //        DocumentRange range_ = richEdit.Document.CreateRange(start, length);
        //        getParoles.Add(new Chant() { Parole = richEdit.Document.GetText(range_) });
        //        start = found[i].End.ToInt();
        //    }
        //    else
        //    {
        //        int length = richEdit.Document.Range.End.ToInt() - found[i].End.ToInt();
        //       DocumentRange range_ = richEdit.Document.CreateRange(start, length);
        //        getParoles.Add(new Chant() { Parole = richEdit.Document.GetText(range_) });
        //        //start = found[i].End.ToInt();
        //    }

        //}
    }
}
